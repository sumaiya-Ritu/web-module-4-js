var bottleColor = 'yellow';
var waterQuantity = 1;
var isFull = false;

// array
var items = ['bottle', 'mug', 'paper', 'pen'];
items.indexOf('paper');
items.indexOf('logens'); //-1
items.push('enevelope');
items.push('watch');
items.pop();

// conditionals
if (items.length >= 4) {
    console.log("lalalal");
}
else if (items.length == 4) {
    console.log('ek dozoen dim');
}
else {
    console.log("faltu");
}